#include <stdio.h>  /* preprocessor command */

int main() {
    /* content of the main function */
    printf("Hello, World!\n");
    printf("The size of int is %lu.\n", sizeof(int));
    return 0;
}
